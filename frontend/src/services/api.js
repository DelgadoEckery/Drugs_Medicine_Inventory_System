import axios from 'axios'; const api=axios.create({baseURL:import.meta.env.VITE_API_URL||'http://127.0.0.1:8000/api',timeout:10000}); api.interceptors.request.use(c=>{const t=localStorage.getItem('inventory_token');if(t)c.headers.Authorization=`Bearer ${t}`;return c}); export const messageFor=e=>e.response?.data?.message||(e.code==='ECONNABORTED'?'The request timed out.':'Unable to reach the server. Please try again.'); export default api;